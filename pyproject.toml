[project]
name = "agenttoolkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "py-llmify[openai]>=0.8.0",
    "pydantic>=2.13.4",
]

[tool.black]
line-length = 88
